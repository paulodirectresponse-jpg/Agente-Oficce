import {Router} from 'express';import {openAgentOfficeDatabase} from '../agent-office/database.js';import {OrchestratorGateway} from '../agent-office/orchestratorGateway.js';
export const v3OrchestratorRouter=Router();
v3OrchestratorRouter.post('/route',async(req,res)=>{const db=openAgentOfficeDatabase();try{const body=req.body??{};const project=String(body.project_id||'');if(!db.connection.prepare('SELECT 1 FROM projects WHERE id=?').get(project)){res.status(404).json({ok:false,error:{code:'PROJECT_NOT_FOUND',message:'PROJECT_NOT_FOUND'}});return}const result=await new OrchestratorGateway(db.connection).route({project_id:project,conversation_id:typeof body.conversation_id==='string'?body.conversation_id:null,user_message_id:typeof body.user_message_id==='string'?body.user_message_id:null,message:String(body.message||''),target:typeof body.target==='string'?body.target:'auto',continuation_agent_id:typeof body.continuation_agent_id==='string'?body.continuation_agent_id:null});res.json({ok:true,data:result})}catch(e){const code=e instanceof Error&&/^[A-Z0-9_]+$/.test(e.message)?e.message:'ORCHESTRATOR_ROUTE_FAILED';res.status(code.endsWith('UNAVAILABLE')?409:400).json({ok:false,error:{code,message:code}})}finally{db.connection.close()}});
v3OrchestratorRouter.get('/runs/:projectId',(req,res)=>{const db=openAgentOfficeDatabase();try{const rows=db.connection.prepare('SELECT * FROM orchestration_runs WHERE project_id=? ORDER BY created_at DESC LIMIT 100').all(req.params.projectId) as any[];res.json({ok:true,data:rows.map(r=>({...r,decision:JSON.parse(r.decision_json),error:r.error_json?JSON.parse(r.error_json):null}))})}finally{db.connection.close()}});
